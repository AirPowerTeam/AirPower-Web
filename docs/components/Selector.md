# Selector 选择器（内部组件）

选择器弹窗组件，为 `ASelect` 的内部实现组件。

## 引入

此组件为内部组件，通常不直接使用，请通过 `ASelect` 组件使用。

## 说明

`Selector.vue` 是 `ASelect` 组件的内部实现，用于显示选择器弹窗。它封装了表格、分页、搜索等功能。

## 注意事项

- 此组件为内部组件，不建议直接使用
- 请使用 `ASelect` 组件进行选择操作
- 组件通过 `ASelect` 的 props 接收参数
